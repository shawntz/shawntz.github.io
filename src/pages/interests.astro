---
import Layout from '../layouts/Layout.astro';
import SpotifyPlaylist from '../components/SpotifyPlaylist.astro';
import Books from '../components/Books.astro';
import Pinterest from '../components/Pinterest.astro';
---

<Layout
  title="Interests"
  description="Music, books, and visual inspiration that fuel my creativity and shape my perspective."
>
  <!-- Hero Section -->
  <section class="relative pt-32 pb-16 lg:pt-40 lg:pb-24 overflow-hidden">
    <div class="max-w-[1800px] mx-auto px-8 sm:px-12 lg:px-20 xl:px-28 2xl:px-32">
      <div class="max-w-4xl">
        <!-- Label -->
        <div class="inline-flex items-center gap-2 mb-6 animate-on-scroll opacity-0">
          <div class="w-2 h-2 rounded-full bg-gradient-to-r from-[#12574A] to-[#2d8a6e]"></div>
          <span class="text-xs font-mono uppercase tracking-wider text-tertiary">
            Beyond the Code
          </span>
        </div>

        <!-- Heading -->
        <h1 class="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-display font-bold mb-6 animate-on-scroll opacity-0" style="animation-delay: 100ms">
          <span class="gradient-text">Interests</span>
        </h1>

        <!-- Description -->
        <p class="text-xl sm:text-2xl text-secondary max-w-2xl leading-relaxed animate-on-scroll opacity-0" style="animation-delay: 200ms">
          The music I listen to, the books I read, and the visual inspiration I collect—these are the things that fuel my creativity and shape how I think about design, technology, and life.
        </p>
      </div>
    </div>
  </section>

  <!-- Music Section -->
  <SpotifyPlaylist />

  <!-- Books Section -->
  <Books />

  <!-- Pinterest Boards Section -->
  <Pinterest />
</Layout>

<style>
  .gradient-text {
    background: linear-gradient(135deg, #12574A 0%, #2d8a6e 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  :global(.dark) .gradient-text {
    background: linear-gradient(135deg, #4ade80 0%, #2d8a6e 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
